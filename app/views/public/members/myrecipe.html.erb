<%= render "public/categories/category", categories: @categories %>
  <h3 class="text-center title">All my recipes</h3>
    <%= render "myrecipe", recipes: @recipes %>
    <div class="d-flex justify-content-center">
      <%= paginate @recipes %>
    </div>